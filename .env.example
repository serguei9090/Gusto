# Gusto - Environment Variables
# Copy this file to .env and fill in the values.

# Application Mode: "community" (default) or "pro" (enables paid modules)
VITE_APP_MODE=community

# Database Mode: "local" (default, SQLite) or "remote" (requires VITE_DB_URL)
VITE_DB_MODE=local

# Remote Database URL (only required when VITE_DB_MODE=remote)
# Example: postgres://user:password@host:5432/gusto
# VITE_DB_URL=
