pre-commit:
  parallel: true
  commands:
    frontend-quality:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bunx @biomejs/biome check --write {staged_files}
    typecheck:
      glob: "*.{ts,tsx}"
      run: bun run type-check
